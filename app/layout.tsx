import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Peter Nguyen — Software Engineer",
  description:
    "Peter Nguyen — Software Engineer bridging hardware, systems, and scalable web applications. C++, TypeScript, React, Next.js, GCP, Docker, and more.",
  openGraph: {
    title: "Peter Nguyen — Software Engineer",
    description:
      "Bridging hardware, systems, and scalable web applications.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
